version: "3.9"

services:
  web:
    build: .
    container_name: quiz-master-web
    ports:
      - "5000:5000"
    environment:
      - FLASK_APP=app.py
    # If you want live code reload you can add:
    # volumes:
    #   - .:/app
